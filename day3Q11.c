#include<stdio.h>
 int main(){
  int num1,num2,gcd;
  printf("enter the two numbers ");
  scanf("%d%d", &num1, &num2);
  for(int i=1; i<=num1,&num2;i++){
    if(num1%1==0 && num2%i==0)
    gcd=i
  }
  printf("gcd of %d and %d is %d",num1,num2);
    return 0;
}